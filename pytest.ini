[pytest]
testpaths = tests
python_files =
    test_base_mount_bridge.py
    test_intent_router.py
    test_tts_spoken_numbers.py
    test_iris_post.py
markers =
    hardware_free: tests that must run without live IRIS hardware
addopts = --strict-markers
