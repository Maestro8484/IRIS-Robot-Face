# IRIS Pending Work Review — 2026-03-26

---

## 1. PENDING TEENSY FLASHES

### What needs to be uploaded

The following code changes are **committed to git but NOT yet flashed** to the Teensy.
A `pio run -t upload` + PROG button press is required.

| Commit | Change | Impact |
|---|---|---|
| `357e41f` | Added MAX7219 mouth driver (`mouth.h` v1 — single 8x8) | MOUTH:x serial command added |
| `a9faa0d` | assistant.py integration (Pi4 side only — no firmware delta) | None on Teensy |
| `60062dc` | **mouth.h v2: 32x8 (4-chip chain), 9 expressions, row reversal, 2µs EMI delay** | ACTIVE CHANGE — mouth matrix will not work correctly without this flash |
| `60062dc` | **main.cpp: removed `face.is_facing` check** | Face tracking now locks on profile/angled faces |

**Bottom line:** Two functional changes are unflashed — the 32x8 mouth driver rewrite and the is_facing removal. The mouth matrix hardware will display garbage or nothing until the new firmware is on the Teensy.

### Flash command
```
pio run -t upload
```
Then press PROG button on Teensy 4.0.
Verify at serial monitor 115200: send `MOUTH:1` → should light HAPPY arc across 32 columns.

---

## 2. SLEEP STARFIELD — STATUS

### Was it ever implemented?

**No. Zero lines of starfield code exist in the firmware.** Searched all `.cpp` and `.h` files for:
`starfield`, `renderSleep`, `sleepDisplay`, `deepspace`, `crescent`, `nebula`, `shootingStar`, `ZZZ` — all returned empty.

### What the current firmware does during EYES:SLEEP

```cpp
// main.cpp line 297
if (eyesSleeping) return;   // bare return — both displays stay black, nothing rendered
```

That's it. Black screens, loop exits immediately.

### Where the design lives

**Source conversation:** "2026-03-25 (Planning session — Claude Chat)" — this is a Claude.ai Chat session (not Claude Code), not recoverable from git history. The design output was manually captured into the snapshot.

**Full spec (from SNAPSHOT_2026-03-25.md, Pending Item #2):**
> On `EYES:SLEEP`, replace black screens with deep space scene: crescent moon, nebula color wash, 3-depth star field (25 stars), shooting stars on staggered timers, expanding pulse rings, drifting ZZZ chain. Custom `renderSleepFrame()` called from `loop()` when `eyesSleeping == true` instead of bare `return`. APA102 breathes dim indigo (floor=1, peak=6) during sleep. Both eyes slightly varied, not mirrored clones.

**Implementation hook** (where code goes in main.cpp):
```cpp
// Replace line 297:
if (eyesSleeping) { renderSleepFrame(); return; }
```

---

## 3. CRON SLEEP SCHEDULE + WOL WAKEWORD — STATUS

### Was it ever implemented?

**No.** Checked Pi4:
- `iris_sleep.py` — **does not exist**
- `iris_wake.py` — **does not exist**
- `SLEEP_MODE` flag file — **does not exist**
- `crontab -l` — **empty** (no cron jobs)

### What the Pi4 currently does at night

Nothing. assistant.py runs 24/7. Jarvis stays fully awake. No sleep mode, no cron schedule.

### Full spec (from SNAPSHOT_2026-03-25.md, Pending Item #3)

**9:00 PM cron → `iris_sleep.py`:**
- Set `/home/pi/SLEEP_MODE` flag file
- Send `EYES:SLEEP\n` + `MOUTH:SLEEP\n` to Teensy via `/dev/ttyACM0`
- Piper TTS says "Goodnight."
- assistant.py detects flag → skips LLM/TTS/ElevenLabs pipeline

**7:30 AM cron → `iris_wake.py`:**
- Remove `SLEEP_MODE` flag file
- Send `EYES:WAKE\n` + `MOUTH:NEUTRAL\n` to Teensy
- Resume full assistant

**Wakeword-during-sleep logic (stays in `assistant.py`):**
- OWW wakeword listener stays active overnight (port 10400, no Gandalf needed)
- If `hey_jarvis` fires while SLEEP_MODE flag is present:
  1. WoL magic packet → GandalfAI MAC: `A4:BB:6D:CA:83:20`
  2. Poll `http://192.168.1.3:11434` until Ollama responds (up to ~60s)
  3. Remove SLEEP_MODE flag
  4. Send `EYES:WAKE\n` + `MOUTH:NEUTRAL\n`
  5. Resume normal conversation turn

**Decision recorded in planning session:** Cron-based sleep preferred over "local phrase handler" (e.g. saying "go to sleep").

---

## 4. IMPLEMENTATION ORDER (recommended)

| Step | Task | Where | Effort |
|---|---|---|---|
| 1 | Flash current firmware (32x8 mouth + face tracking fix) | Desktop → Teensy | 2 min |
| 2 | Build starfield `renderSleepFrame()` in firmware | `src/main.cpp` or new `src/sleep_display.h` | Medium |
| 3 | Flash updated firmware | Desktop → Teensy | 2 min |
| 4 | Write `iris_sleep.py` + `iris_wake.py` on Pi4 | SSH | Small |
| 5 | Add SLEEP_MODE check to `assistant.py` + WoL wakeword handler | SSH | Medium |
| 6 | Set crontab on Pi4 (9PM/7:30AM) | SSH | 5 min |
| 7 | Persist all Pi4 files to SD | SSH | 2 min |

---

## 5. WHAT'S IN CLAUDE HISTORY

The starfield design and cron architecture were **designed in a Claude.ai Chat session on 2026-03-25** — not a Claude Code session and not recoverable from git. The full spec was manually captured into SNAPSHOT_2026-03-25.md Section 10 ("Planning session — Claude Chat") and Pending Items #2 and #3. That snapshot is the authoritative source. No additional detail exists in Claude Code history beyond what's in the snapshot.
